/**
 * @file lite_allocator.hpp
 * @brief Convenience header for LiteAllocator
 *
 * LiteAllocator is defined in runtime.hpp as part of the core lite runtime.
 * This header exists so that downstream code can include <tenzor/lite/lite_allocator.hpp>
 * when only the allocator interface is needed.
 */

#pragma once

#include "runtime.hpp"
